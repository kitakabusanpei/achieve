<div class="container">
  <div class="wrapper col-md-8 col-md-offset-2 col-sm-offset-1 col-sm-9 col-sm-offset-1">
    <h2>お知らせ</h2>
    <ul>
      <% @notifications.each do |notification| %>
      <hr>
        <li>
          <p class="h5 left bg-info"><span><%= notification.read ? '既読' : '未読' %></span></p>
          <p class="left">
            <%= notification.comment.user.try(:name) %>さんが
            あなたのブログ（<%= link_to "#{notification.comment.blog.title}", blog_path(notification.comment.blog, notification_id: notification.id) %>）にコメントしました。
          </p>
          <p class="right"><%= notification.created_at.strftime("%Y/%m/%d %p %X") %></p>
        </li>
      <% end %>
    </ul>
  </div>
</div>
